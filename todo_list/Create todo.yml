info:
  name: Create todo
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/todos/"
  body:
    type: json
    data: |-
      {
        "title": "Comprar leche",
        "description": "Leche desnatada"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
