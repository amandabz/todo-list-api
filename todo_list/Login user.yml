info:
  name: Login user
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/login/"
  body:
    type: json
    data: |-
      {
        "email": "carmen@test.com",
        "password": "12345678aa"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        var data = res.getBody();
        bru.setEnvVar("token", data.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
