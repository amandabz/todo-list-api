info:
  name: Delete todos
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/todos/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
